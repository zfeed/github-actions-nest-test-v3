import bet from './schemas/bet';
import betEntity from './schemas/entity';
import status from './schemas/status';

const options = {
    entities: [bet, betEntity, status]
};

export default options;
